export interface SimplebenchOptions { model?: string; allModels: boolean; writeArtifact: boolean; }
export interface RequestMetrics { requestCount: number; retryCount: number; wallTimeMs: number; timeToAnswerMs: number; timeToFirstTokenMs: number | null; startedAt: string | null; finishedAt: string | null; inputTokens: number | null; outputTokens: number | null; totalTokens: number | null; outputTokensPerSecond: number | null; toolCalls: Array<{ name: string; arguments?: unknown }>; }
export interface TestRecord { id: string; kind: "reasoning" | "instructions" | "tools"; category?: string; prompt: string; response: string | null; expectedAnswer?: string | string[]; extractedAnswer?: string; score: string; passed: boolean; error: string | null; metrics: RequestMetrics; }
export interface RunArtifact { schemaVersion: 1; benchmark: { name: "opl-simplebench"; model: string; provider: string; providerKind: string; startedAt: string; finishedAt: string; wallTimeMs: number; artifactEnabled: boolean; }; tests: TestRecord[]; summary: Record<string, unknown>; }
